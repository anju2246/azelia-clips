/**
 * Stub for v0.1.0 — no Pro tier in local-first MVP. Returns null.
 * Imported by OnboardingWizard + DashboardController until those are
 * refactored to drop the reference (v0.1.1 cleanup).
 */
export function ProUpgradeCard(_props: Record<string, unknown>) {
    return null;
}
export default ProUpgradeCard;
