/**
 * Stub for local-first MVP — YouTube historical sync disabled in v0.1.0.
 * Returns in v0.1.1 with local-only persistence.
 */
export function RetroactiveSyncModal(_props: Record<string, unknown>) {
    return null;
}
export default RetroactiveSyncModal;
